name: Sync kenzok8 Packages

on:
  schedule:
    - cron: '10 */6 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync-kenzok8:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.REPO_TOKEN }}

      - name: Setup Git
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"

      - name: Install deps
        run: pip install toml

      - name: Backup workflows
        run: |
          mkdir -p /tmp/backup
          cp -r .github/workflows /tmp/backup/
          echo "Workflows backed up"

      - name: Clean repo (keep workflows)
        run: |
          # 删除除 .git 和 .github 外的所有内容
          find . -maxdepth 1 -not -name '.git' -not -name '.github' -exec rm -rf {} + 2>/dev/null || true
          echo "Repository cleaned"

      - name: Sync repositories
        run: |
          python3 << 'EOF'
          import toml, os, subprocess, tempfile, shutil
          
          config = toml.load('.github/workflows/sync-config.toml')
          
          # 首先处理需要覆盖的目录，记录哪些目录会被覆盖
          override_dirs = set()
          for repo in config['repositories']:
              if repo.get('target_dir') and repo['target_dir'] != '.':
                  override_dirs.add(repo['target_dir'])
          
          for repo in config['repositories']:
              print(f"🔄 Syncing {repo['name']}...")
              
              with tempfile.TemporaryDirectory() as tmpdir:
                  # 下载仓库
                  zip_url = f"{repo['url']}/archive/refs/heads/{repo.get('branch', 'main')}.zip"
                  zip_file = os.path.join(tmpdir, "repo.zip")
                  
                  subprocess.run(['curl', '-sL', '-o', zip_file, zip_url], check=True)
                  subprocess.run(['unzip', '-q', zip_file, '-d', tmpdir], check=True)
                  
                  # 找到解压目录
                  extracted = [d for d in os.listdir(tmpdir) 
                             if os.path.isdir(os.path.join(tmpdir, d)) and not d.startswith('__')][0]
                  src_dir = os.path.join(tmpdir, extracted)
                  
                  if repo.get('type') == 'partial':
                      # 部分同步
                      src_path = os.path.join(src_dir, repo['source_path'])
                      target_dir = repo['target_dir']
                      if os.path.exists(src_path):
                          os.makedirs(target_dir, exist_ok=True)
                          # 检查并删除目标目录（如果存在且需要覆盖）
                          if os.path.exists(target_dir):
                              print(f"🗑️ Removing existing directory: {target_dir}")
                              shutil.rmtree(target_dir)
                          # 忽略损坏的符号链接
                          shutil.copytree(src_path, target_dir, ignore_dangling_symlinks=True)
                          print(f"✅ Partial sync: {repo['source_path']} -> {target_dir}")
                      else:
                          print(f"❌ Source path not found: {src_path}")
                  else:
                      # 完整同步
                      target_dir = repo.get('target_dir', '.')
                      if target_dir and target_dir != '.':
                          os.makedirs(target_dir, exist_ok=True)
                          # 检查并删除目标目录（如果存在且需要覆盖）
                          if os.path.exists(target_dir):
                              print(f"🗑️ Removing existing directory: {target_dir}")
                              shutil.rmtree(target_dir)
                          # 忽略损坏的符号链接
                          shutil.copytree(src_dir, target_dir, ignore_dangling_symlinks=True)
                          print(f"✅ Full sync to: {target_dir}")
                      else:
                          # 同步到根目录 - 特殊处理可能被覆盖的目录
                          for item in os.listdir(src_dir):
                              src_item = os.path.join(src_dir, item)
                              dst_item = os.path.join('.', item)
                              
                              # 如果这个目录后续会被其他仓库覆盖，跳过同步
                              if os.path.isdir(src_item) and item in override_dirs:
                                  print(f"⏩ Skipping {item} (will be overridden by another repository)")
                                  continue
                              
                              if os.path.isdir(src_item):
                                  if os.path.exists(dst_item):
                                      shutil.rmtree(dst_item)
                                  # 忽略损坏的符号链接
                                  shutil.copytree(src_item, dst_item, ignore_dangling_symlinks=True)
                              else:
                                  try:
                                      shutil.copy2(src_item, dst_item)
                                  except Exception as e:
                                      print(f"⚠️ 跳过文件 {src_item}: {e}")
                          print("✅ Full sync to root")
          
          print("🎉 All repositories synced!")
          EOF

      - name: Restore workflows
        run: |
          if [ -d "/tmp/backup/workflows" ]; then
            mkdir -p .github/workflows
            cp -r /tmp/backup/workflows/* .github/workflows/
            echo "✅ Workflows restored"
          fi

      - name: Replace files
        run: |
          python3 << 'EOF'
          import toml, urllib.request, os
          
          config = toml.load('.github/workflows/sync-config.toml')
          
          for file in config.get('file_replacements', []):
              os.makedirs(os.path.dirname(file['target_path']), exist_ok=True)
              urllib.request.urlretrieve(file['source_url'], file['target_path'])
              print(f"📄 Replaced {file['target_path']}")
          
          print("✅ File replacements completed")
          EOF

      - name: Verify directory overrides
        run: |
          echo "📁 Checking directory overrides:"
          if [ -d "luci-app-lucky" ]; then
            echo "✅ luci-app-lucky directory exists"
            echo "📊 Contents:"
            find "luci-app-lucky" -type f -name "*.lua" -o -name "*.js" -o -name "*.html" | head -10
          else
            echo "❌ luci-app-lucky directory not found"
          fi

      - name: Commit and push
        run: |
          git add .
          if git diff --staged --quiet; then
            echo "📝 No changes to commit"
          else
            git commit -m "Auto-sync: $(date +'%Y-%m-%d %H:%M')"
            git push
            echo "✅ Changes pushed"
          fi

      - name: Cleanup old workflow runs
        uses: Mattraks/delete-workflow-runs@v2
        with:
          token: ${{ secrets.REPO_TOKEN }}
          repository: ${{ github.repository }}
          retain_days: 2
          keep_minimum_runs: 6
